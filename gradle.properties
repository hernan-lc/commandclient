# Build-time settings only. Runtime settings live in config/commandapi.json
# (see docs/API.md); Minecraft target metadata lives in versions.json.

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=false
